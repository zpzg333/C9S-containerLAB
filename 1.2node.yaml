apiVersion: clabernetes.containerlab.dev/v1alpha1
kind: Topology
metadata:
  name: evpn-multihomming
  namespace: test-network
spec:
  imagePull:
    pullSecrets:
      - ghcr-secret
  naming: non-prefixed
  definition:
    containerlab: |-
          # topology documentation: http://containerlab.dev/lab-examples/srl-ceos/
          name: evpn-multihomming
          topology:
            kinds:
              arista_ceos1:
                image: ghcr.io/zpzg333/ceos:4.34.4f   # 레포 주소 
                env:
                  CLAB_MGMT_VRF: MGMT
              arista_ceos2:
                image: ghcr.io/zpzg333/ceos:4.22.0f   # 레포 주소 
                env:
                  CLAB_MGMT_VRF: MGMT
            nodes:
              leaf1:
                kind: arista_ceos1
              leaf2:           
                kind: arista_ceos2
            links:
              #spine to leaf
              - endpoints: ["leaf1:eth1", "leaf2:eth1"]
              - endpoints: ["leaf1:eth2", "leaf2:eth2"]
